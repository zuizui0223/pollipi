{
  "name": "@visit-monitor/coordinator",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "PYTHONPATH=. uvicorn main:app --host 0.0.0.0 --port 8001 --reload",
    "start": "PYTHONPATH=. uvicorn main:app --host 0.0.0.0 --port 8001"
  }
}
