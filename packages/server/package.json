{
  "name": "@visit-monitor/server",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "PYTHONPATH=src uvicorn visit_monitor_server.main:app --host 0.0.0.0 --port 8000 --reload",
    "start": "PYTHONPATH=src python -m visit_monitor_server.main",
    "build": "python ../../tools/build_server_artifact.py",
    "test": "python -m pytest"
  }
}
